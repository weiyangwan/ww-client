/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ActivityCollapseListComponent } from './activity-collapse-list.component';

describe('Component: ActivityCollapseList', () => {
  it('should create an instance', () => {
    let component = new ActivityCollapseListComponent();
    expect(component).toBeTruthy();
  });
});
