/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ItineraryResourcesComponent } from './itinerary-resources.component';

describe('Component: ItineraryResources', () => {
  it('should create an instance', () => {
    let component = new ItineraryResourcesComponent();
    expect(component).toBeTruthy();
  });
});
